{
  "name": "asidebot",
  "version": "0.0.1",
  "description": "discord bot for moving one or more people to a voice channel for a temporary side conversation, and then moving them back to the channel they came from",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gregory Propst",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^11.4.2"
  },
  "devDependencies": {
    "babel-preset-env": "^1.7.0",
    "babel-register": "^6.26.0"
  }
}
